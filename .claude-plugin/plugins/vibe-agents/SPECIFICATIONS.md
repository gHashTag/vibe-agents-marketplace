# 📋 Спецификации создания агентов Vibee

## 🎯 Философия агентной системы

### Основные принципы

1. **🐝 Роевой интеллект** - координация множества агентов-пчелок через централизованную систему
2. **🤖 Автономность** - агенты работают самостоятельно в непрерывном цикле до успеха
3. **📏 "7 раз отмерь"** - тщательное планирование перед каждым действием
4. **⚡ Функциональное программирование** - чистые функции, иммутабельность, без побочных эффектов
5. **🧪 TDD** - тесты пишутся ПЕРЕД кодом (100% покрытие)

---

## 🏗️ Стандартная структура агента

### YAML Frontmatter (обязательно!)

```yaml
---
name: vibe-[name]                    # Имя агента (совпадает с именем файла)
agent_id: vibe-[name]                # Уникальный идентификатор
description: [description]           # Что делает агент и когда активируется
tools: Read, Grep, Bash, WebFetch, Write, Edit  # Доступные инструменты
model: sonnet                        # Рекомендуемая LLM модель

# Vibee специфичные поля
vibe_role: [role]                    # Роль: queen-bee | specialist-agent | task-manager
vibe_domain: [domain]                # Специализация: framework | implementation | testing | security | devops | monitoring | observability | ai-llm | cicd | roi-analytics
vibe_emoji: [emoji]                  # Эмодзи для визуализации
vibe_color: "[hex-color]"            # Цвет в HEX формате

# Критически важно для Vibee
vibe_chain_priority: [number]        # Приоритет в цепочке (1-10, где 1 = самый высокий)
vibe_autonomous: true                # Должен работать автономно (true/false)
vibe_swarm_leader: [agent-id]        # Кто координирует этого агента (null для queen-bee)
---

# Описание агента

## 🎯 Когда активируется
[Описание когда и как активируется агент]

## 🔧 Что делает
[Подробное описание функций агента]

## 🐝 Координация с другими агентами
[Как взаимодействует с роем]

## 📋 Шаги работы (обязательно)

### Шаг 1: Анализ задачи
- ✅ Анализ требований
- ✅ Понимание контекста
- ✅ Определение критериев успеха

### Шаг 2: Поиск паттернов
- ✅ Поиск похожих решений в кодовой базе
- ✅ Изучение лучших практик
- ✅ Анализ существующих паттернов

### Шаг 3: Проверка зависимостей
- ✅ Проверка доступности модулей
- ✅ Валидация типов данных
- ✅ Проверка внешних сервисов

### Шаг 4: Планирование
- ✅ Составление детального плана
- ✅ Определение шагов
- ✅ Оценка рисков

### Шаг 5: Валидация плана
- ✅ Проверка корректности
- ✅ Поиск пропусков
- ✅ Финальная проверка

### Шаг 6: Выполнение
- ✅ Автономная работа до успеха
- ✅ Исправление ошибок
- ✅ Никогда не выходить до успешного завершения

### Шаг 7: Тестирование
- ✅ 100% покрытие тестами
- ✅ Проверка всех критериев
- ✅ Валидация результата

## 🧪 TDD - Test-Driven Development

### Фаза 1: Красный тест
- ✅ Пишем тест ПЕРЕД кодом
- ✅ Тест должен падать
- ✅ Определяем API

### Фаза 2: Зеленый тест
- ✅ Пишем минимальный код
- ✅ Тест должен пройти
- ✅ Никакой лишней логики

### Фаза 3: Рефакторинг
- ✅ Улучшаем код
- ✅ Тесты остаются зелеными
- ✅ Убираем дублирование

## ⚡ Функциональное программирование

### Типы данных

```typescript
// TaskEither для асинхронных операций
type TaskEither<E, A> = () => Promise<Either<E, A>>

// Either для синхронных операций
type Either<E, A> = Left<E> | Right<A>

// Статус результата операции
type OperationStatus = 'pending' | 'in_progress' | 'completed' | 'failed'

// Контекст агента
interface AgentContext {
  agentId: string
  taskId: string
  startTime: Date
  attempts: number
  maxAttempts: number
}

// Результат операции агента
interface AgentResult<T> {
  success: boolean
  data?: T
  error?: Error
  context: AgentContext
}
```

### Функции-утилиты

```typescript
// Композиция функций
const pipe = <T, R>(...fns: Array<(arg: T) => R>) => (value: T): R =>
  fns.reduce((acc, fn) => fn(acc), value)

// Создание успешного результата
const right = <T>(value: T): Right<T> => ({ _tag: 'Right', value })

// Создание ошибки
const left = <T>(error: T): Left<T> => ({ _tag: 'Left', error })

// Цепочка операций
const chain = <E, A, B>(fn: (a: A) => Either<E, B>) => (either: Either<E, A>): Either<E, B> =>
  either._tag === 'Right' ? fn(either.value) : either

// Обработка побочных эффектов (НЕ в чистых функциях!)
const tap = <T>(fn: (value: T) => void) => (either: Either<any, T>): Either<any, T> => {
  if (either._tag === 'Right') fn(either.value)
  return either
}
```

### Пример агента в функциональном стиле

```typescript
import { pipe, chain, right, left } from '@/utils/functional'

export const analyzeTask = (task: string): Either<Error, TaskAnalysis> => {
  // Чистая функция - анализирует задачу
  if (!task || task.trim().length === 0) {
    return left(new Error('Задача не может быть пустой'))
  }

  return right({
    description: task,
    type: classifyTaskType(task),
    priority: extractPriority(task),
    dependencies: extractDependencies(task),
  })
}

export const findPatterns = (analysis: TaskAnalysis): TaskEither<Error, FoundPatterns> => {
  return async () => {
    // Ищем существующие решения
    const patterns = await searchCodebase(analysis)
    return right({
      similarFunctions: patterns,
      existingSolutions: await findSimilarSolutions(analysis),
      bestPractices: await getBestPractices(analysis.type),
    })
  }
}

export const createAgent = (spec: AgentSpec): TaskEither<Error, Agent> => {
  return pipe(
    analyzeTask(spec.description),
    chain(analysis =>
      pipe(
        findPatterns(analysis),
        chain(patterns =>
          pipe(
            createAgentStructure(analysis, patterns),
            chain(structure =>
              pipe(
                writeAgentFiles(structure),
                chain(() => createTests(structure)),
                chain(() => createDocumentation(structure))
              )
            )
          )
        )
      )
    )
  )
}
```

---

## 🐝 Архитектура роя агентов

### Уровни агентов

```
УРОВЕНЬ 1: QUEEN BEE (Королева)
├── vibe-lead              👑 Координация всех агентов
└── Специализированные агенты

УРОВЕНЬ 2: SPECIALISTS (Специалисты)
├── vibe-spec              📋 Создание спецификаций
├── vibe-tester            🧪 Написание тестов
├── vibe-critic            🎭 Валидация результатов
├── vibe-coder             💻 Разработка
├── vibe-typescript        📘 TypeScript эксперт
├── vibe-tasker            ✅ Менеджер задач
├── vibe-security          🔐 Безопасность
├── vibe-cicd              🔄 CI/CD
├── vibe-devops            🚀 DevOps
├── vibe-roi               💰 ROI аналитика

УРОВЕНЬ 3: DOMAIN EXPERTS (Эксперты доменов)
├── vibe-elizaos           ⚡ ElizaOS эксперт
├── vibe-ai-llm            🤖 AI/LLM провайдер
├── vibe-mcp               🔌 MCP интеграция
├── vibe-sentry            📡 Мониторинг
└── vibe-langfuse          📊 Наблюдаемость
```

### Цепочка арбитрации

```
User Request
    ↓
[vibe-lead] Координация
    ↓
[vibe-spec] Спецификация
    ↓
[vibe-tester] Тесты
    ↓
[vibe-coder] Реализация
    ↓
[vibe-critic] Валидация
    ↓
[vibe-sentry] Мониторинг
    ↓
Отчет пользователю
```

### Автономный цикл агента

```typescript
export async function autonomousAgentLoop<T>(
  task: Task<T>,
  config: Partial<AutonomousLoopConfig> = {}
): Promise<TaskEither<Error, TaskResult>> {
  const finalConfig: AutonomousLoopConfig = {
    maxAttempts: 10,
    retryDelay: 1000,
    backoffMultiplier: 2,
    ...config
  }

  let attempt = 0

  while (attempt < finalConfig.maxAttempts) {
    try {
      // Анализ задачи
      const analysis = await analyzeTask(task)
      if (analysis.isLeft()) {
        attempt++
        await sleep(finalConfig.retryDelay * attempt)
        continue
      }

      // Поиск паттернов
      const patterns = await findPatterns(analysis.value)
      if (patterns.isLeft()) {
        attempt++
        await sleep(finalConfig.retryDelay * attempt)
        continue
      }

      // Выполнение
      const result = await executeTask(task, analysis.value, patterns.value)
      if (result.isRight()) {
        return right(result.value)
      }

      // Ошибка - продолжаем цикл
      attempt++
      await sleep(finalConfig.retryDelay * attempt)
    } catch (error) {
      attempt++
      if (attempt >= finalConfig.maxAttempts) {
        return left(error as Error)
      }
      await sleep(finalConfig.retryDelay * attempt)
    }
  }

  return left(new Error(`Не удалось выполнить задачу за ${finalConfig.maxAttempts} попыток`))
}
```

---

## 📝 Чек-лист создания нового агента

### Обязательные шаги

- [ ] **Создать файл агента** в `.claude/agents/vibe-[name].md`
- [ ] **Добавить YAML frontmatter** со всеми обязательными полями
- [ ] **Описать активацию** - когда и как вызывается
- [ ] **Описать функции** - что делает агент
- [ ] **Описать координацию** - как взаимодействует с роем
- [ ] **Написать 7 шагов работы** (анализ → поиск паттернов → проверка зависимостей → планирование → валидация → выполнение → тестирование)
- [ ] **Добавить примеры использования** в документации
- [ ] **Создать тесты** в `tests/agents/vibe-[name].test.ts`
- [ ] **Добавить в классификацию** `VIBE_AGENTS_CLASSIFICATION.md`
- [ ] **Протестировать активацию** - проверить что агент правильно активируется

### Критичные требования

- [ ] **100% функциональный стиль** - никаких классов, мутаций, побочных эффектов в чистых функциях
- [ ] **TaskEither/Either** для всех асинхронных/синхронных операций
- [ ] **TDD** - тесты ПЕРЕД кодом
- [ ] **Автономность** - работает до успешного завершения без выхода
- [ ] **Планирование** - "7 раз отмерь" перед любым действием
- [ ] **Типизация** - строгие TypeScript типы
- [ ] **Логирование** - через VIBE-SENTRY
- [ ] **Документация** - подробное описание в README

---

## 🎨 Цветовая схема агентов

```css
/* Chain Agents */
vibe-lead:     #FFA500  /* OrangeGold */
vibe-spec:     #4CAF50  /* LightGreen */
vibe-tester:   #9C27B0  /* Purple */
vibe-critic:   #FFD700  /* Gold */

/* Specialist Agents */
vibe-coder:     #00E676  /* Green */
vibe-typescript: #007ACC /* TypeScript Blue */
vibe-tasker:    #2196F3  /* Blue */
vibe-security:  #F44336  /* DeepRed */
vibe-cicd:      #FF9800  /* Orange */
vibe-devops:    #00BCD4  /* Cyan */
vibe-roi:       #FFC107  /* Amber */

/* Domain Experts */
vibe-elizaos:   #00D4FF  /* Cyan */
vibe-ai-llm:    #FF00FF  /* Magenta */
vibe-mcp:       #8A2BE2  /* BlueViolet */
vibe-sentry:    #FF1744  /* Red */
vibe-langfuse:  #FF6B00  /* Orange */
```

---

## 🚀 Быстрый старт

### 1. Создание нового агента

```bash
# Копируем шаблон
cp .claude/agents/vibe-template.md .claude/agents/vibe-new-agent.md

# Редактируем YAML frontmatter
vim .claude/agents/vibe-new-agent.md

# Добавляем описание, функции, примеры
vim .claude/agents/vibe-new-agent.md

# Создаем тесты
cat > tests/agents/vibe-new-agent.test.ts << 'EOF'
import { describe, it, expect } from 'vitest'

describe('vibe-new-agent', () => {
  it('должен активироваться по ключевым словам', () => {
    // TDD тест
  })
})
EOF
```

### 2. Тестирование

```bash
# Запускаем тесты
npm test tests/agents/vibe-new-agent.test.ts

# Проверяем активацию
# В Claude Code: "Как сделать X?"
# Должен активироваться vibe-new-agent
```

### 3. Добавление в цепочку

```typescript
// В main project index.ts
import { vibeNewAgentPlugin } from './vibe-new-agent-plugin'

export const agents = [
  vibeSpecAgent,
  vibeTesterAgent,
  vibeNewAgentPlugin,  // Добавляем в цепочку
  vibeCoderAgent,
  vibeCriticAgent,
]
```

---

**Vibee Agents - Система роевого интеллекта для автономной разработки! 🐝**
